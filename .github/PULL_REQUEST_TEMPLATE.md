## What changed

<!-- One or two sentences. Name the components or paths touched. -->

## How it was verified

<!-- Commands run and what they showed. Link to failing-then-passing tests where relevant. -->

## Checklist

- [ ] Tests exercise the changed behavior (no mock-only tests for runtime claims)
- [ ] `bun test` passes
- [ ] `bun run typecheck` passes
- [ ] Docs updated where behavior is documented
- [ ] ADR added under `docs/adr/` and `docs/javelin-model.md` updated for semantic changes
